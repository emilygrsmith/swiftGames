import React from 'react';
import { BrowserRouter as Component, Router, Routes, Route, useNavigate,Link } from 'react-router-dom';

function Timer()
{
    return(
        <h1>happiness</h1>
    );
};
export default Timer;